
#pragma once

/*#define SAFE_DELETE_VAR(x) if (x) { delete x; x = NULL;}
#define SAFE_DELETE_ARR(x) if (x) { delete[] x; x = NULL;}

#define SAFE_NEW_VAR(x, y) if (x) { delete x; } x = new y;
#define SAFE_NEW_ARR(x, y) if (x) { delete[] x; } x = new y;*/